name: CI

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  quality:
    name: Code Quality
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Type Check
        run: pnpm typecheck

      - name: Lint
        run: pnpm lint

      - name: Format Check
        run: pnpm format:check

      - name: Test
        run: pnpm test:coverage

      - name: Upload Coverage
        uses: codecov/codecov-action@v4
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          files: ./coverage/lcov.info
          fail_ci_if_error: false

  # Build jobs are disabled for scaffold project
  # Uncomment and configure when ready for production deployment
  #
  # Known issues to resolve before enabling:
  # - iOS: react-native-worklets version compatibility with RN 0.76.9
  # - Android: gradle cache needs files from expo prebuild
  # - Web: requires react-native-web and @expo/metro-runtime

  # build_android:
  #   name: Build (Android Debug APK)
  #   runs-on: ubuntu-latest
  #   needs: [quality]
  #   if: github.event_name == 'push'
  #   ...

  # build_ios:
  #   name: Build (iOS Simulator Debug App)
  #   runs-on: macos-latest
  #   needs: [quality]
  #   if: github.event_name == 'push'
  #   ...

  # build_web:
  #   name: Build (Web Export)
  #   runs-on: ubuntu-latest
  #   needs: [quality]
  #   if: github.event_name == 'push'
  #   ...
